name: PR Check

on:
  workflow_dispatch:

  pull_request:
    branches:
      - main

permissions:
  contents: read
  checks: write
  packages: read

jobs:
  test:
    uses: ./.github/workflows/ci-tests.yaml
